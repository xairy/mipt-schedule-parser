create table classes(
    `group` text,
    subgroup integer,

    week_day integer,
    class_number integer,
    class_half integer,

    subjects text,
    locations text,
    teachers text,

    `type` integer,
    raw_data text
);
